#!/bin/sh

# <html><h2>dim</h2>
# <h3>Examples</h3>
# <dl>
# <dt>Show arguments with space in Dim:</dt>
# <dd>
# <pre><code class="language-bash">dim Show Text
# </code></pre>
# </dd>
# </dl>
# </html>
[ $# -eq 0 ] || printf -- '%b' "\e[2m"; printf '%b' "$*"; printf -- '%b' "\e[0m"
